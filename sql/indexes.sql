-- indexes.sql
-- Owner: LANCEA
-- Add indexes here once the views/queries above reveal slow paths (Iter 2 on).

-- TODO examples:
-- CREATE INDEX idx_orders_stall_created ON orders(stall_id, created_at);
-- CREATE INDEX idx_menu_item_stall ON menu_item(stall_id);
-- CREATE INDEX idx_ledger_wallet ON ledger_entry(wallet_id);
