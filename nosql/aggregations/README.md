# Aggregations

Mongo pipelines on the event / review collections:

- `by_stall.js` — totals and counts per stall
- `by_method_hour.js` — PayNow / card / cash by hour of day
- `review_search.js` — text search on review comments
