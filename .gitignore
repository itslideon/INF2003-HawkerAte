# dependencies
node_modules/
__pycache__/
*.pyc
.venv/

# env / secrets
.env
.env.local

# local-only — never commit
INF2003_project_description.pdf
DATABASEPROJ.md

# large / official raw downloads — note the download date in etl/raw/README.md instead
etl/raw/*
!etl/raw/README.md

# build output
dist/
build/

# OS junk
.DS_Store

# personal Claude Code tooling config — not shared project setup
.claude/
